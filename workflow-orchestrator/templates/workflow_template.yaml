name: "My Custom Workflow"
schedule: "09:00" # HH:MM format, 24-hour. Optional if you only run manually.

steps:
  # Example Step 1: Generate a topic
  # - id: step1
  #   skill: news-topic-generator
  #   # Command runs relative to the skill's directory
  #   command: python scripts/generate.py --count 1
  
  # Example Step 2: Use the output
  # - id: step2
  #   skill: copywriting
  #   # {step1.output} is the stdout from the previous step
  #   command: python scripts/write.py --topic "{step1.output}"
